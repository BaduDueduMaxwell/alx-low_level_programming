#include "main.h"
#include <stdio.h>
#include <stdlib.h>

/**
 * print_numbers - prints the numbers from 0-9
 * @void - character
 * Return: prints numbers with _putchar
 */
void print_numbers(void)
{
	int num;
	for (num = 0; num <= 9; num++)
	{
		putchar("%d", num);
	}
	putchar('\n');
}
