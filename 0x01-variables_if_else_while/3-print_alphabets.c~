#include <stdio.h>

/**
 * main - a-z, A-Z
 *
 * Return: 0 if successful
 */

int main(void)
{
	char letter;

	for (letter = 'a'||'A'; letter <= 'z'||'Z'; letter++)
	{
		putchar(letter);
	}
	putchar('\n');

	return (0);
}
